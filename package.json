{
  "name": "velt-chat-sdk-adapter-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Monorepo for @velt-js/chat-sdk-adapter and its example app",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build -w @velt-js/chat-sdk-adapter",
    "test": "npm run test -w @velt-js/chat-sdk-adapter",
    "typecheck": "npm run typecheck -w @velt-js/chat-sdk-adapter"
  },
  "license": "Apache-2.0"
}
